{
  "name": "@inseoul/calculation",
  "version": "0.1.0",
  "private": true,
  "description": "D-Day 계산 공식, 검증 케이스.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc --noEmit",
    "test": "tsc -b && node --test \"test/*.test.mjs\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
