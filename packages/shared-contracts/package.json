{
  "name": "@inseoul/shared-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "InSeoul 공통 타입 및 API Request/Response DTO. FE/BE/AI 계약의 단일 소스.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --noEmit || tsc --noEmit",
    "test": "node --test 2>/dev/null || echo 'no tests yet'",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
